" Python specific settings
"
" 79 is the max length recommended for python
setlocal colorcolumn=80

" Insert space characters whenever I hit <TAB>
setlocal expandtab

" Just make both of these the same, I guess.
setlocal softtabstop=4
setlocal tabstop=4
